import { Section, Video } from '@components'
import { cookieJarParade } from '@data'

const CookieJarParade = () => (
  <Section>
    <Video video={cookieJarParade} />
  </Section>
)

export default CookieJarParade
