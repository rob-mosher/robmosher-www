import { Video, Section } from '@components'
import { winterCarousel } from '@data'

const WinterCarousel = () => (
  <Section>
    <Video video={winterCarousel} />
  </Section>
)

export default WinterCarousel
